# Verification evidence

Local verification performed on 2026-09-13 UTC using Docker Desktop with Linux containers on Windows. These results apply to the application and configuration in this repository; they do not establish production certification.

| Verification | Result |
| --- | --- |
| Java domain and token validation | 14 tests passed |
| Frontend exact decimal parsing, totals and formatting | 14 tests passed |
| Real PostgreSQL/RabbitMQ/Keycloak API integration | 32 checks passed |
| Browser desktop and mobile | Login, creation, deposit, validation, audit, logout and automated accessibility |
| Trivy 0.74.0 application image scan | No HIGH/CRITICAL findings in ledger, audit or web runtime images at scan time |

The integration suite includes account ownership isolation, forged tokens, invalid inputs, precision boundaries, overdraft and overflow rejection, replay equality, idempotency conflicts, concurrent withdrawals, broker interruption, recovery and a verified actual duplicate republish with no extra audit effect. It creates data in the local test environment.

The browser suite checks applicable automated WCAG 2.1 AA rules using axe and detects horizontal page overflow. It is not a complete manual screen-reader or accessibility audit. Test traces are disabled to avoid storing login credentials.

Trivy reports are generated by CI and retained as artifacts. LOW/MEDIUM vulnerabilities and infrastructure images are outside the stated zero HIGH/CRITICAL result. Scanner support for OS lifecycle status is distinct from vulnerability matching; review both when deploying. Run the pipeline again after dependency updates and before using a new build.

Runtime fixes verified in this build include Tomcat 11.0.25, RabbitMQ Java client 5.33.1, NGINX 1.30.4 and updated Alpine packages. The original vulnerable images are not deployment targets. No vulnerability suppression file is used.
